# Linux系统基础

命令行程序

​	终端 terminal

## 常用Linux命令的基础

| 序号 | 命令          | 对应英文             | 作用                       |
| ---- | ------------- | -------------------- | -------------------------- |
| 01   | ls            | list                 | 查看当前文件夹下的内容     |
| 02   | pwd           | print wrok directory | 查看当前所在文件夹         |
| 03   | cd[目录名]    | change directory     | 切换文件夹                 |
| 04   | touch[文件名] | touch                | 如果文件不存在，新建文件夹 |
| 05   | mkdir[目录名] | make directory       | 创建目录                   |
| 06   | rm[文件名]    | remove               | 删除指定的文件名           |
| 07   | clear         | clear                | 清屏                       |

### cd 切换文件夹

- cd /home 绝对路径 以根目录开头
- cd admin 相等路径 不以根目录开头
- cd .. 返回上级目录
- cd ~ 回自己的家目录 /home/admin
- cd - 电视回看功能 （返回根目录再次使用就是返回到上次目录）

### pwd 查看当前目录

### ls 查看目录的内容

- ls 
- ls -l 显示详细列表
- ls -lh 把文件大小以人性化的方式显示
- ls -a 显示所用的文件，包括隐藏文件，隐藏文件是`.`开头的文件
- ll 等于 ls -l
- ls -la /home
- ls *txt 查看当前目录下的所有以txt结尾的文件

### mkdir 创建文件夹

- mkdir tupian 在当前目录下创建文件夹
- mkdir /home/shoajie/tupian 以绝对路径创建文件夹
- mkdir /home/shaojie/a/ tupian -p 如果上级目录不存在，加上-p自动创建父目录
- mkdir a b 在当前目录创建多个文件夹
- mkdir a/{c,d} 在指定目录下创建多个文件夹
- mkdir .abc 以`.`开头是创建隐藏文件夹
- 任何目录下都有至少两个目录`.`和`..` `.`表示当前目录 `..`表示上级目录

### touch 创建空文件夹

- touch abc.txt 在当前目录下创建 如果指定了目录，必须保证上级目录存在
- touch .abc 创建隐藏目录
- gedit abc.txt 使用笔记本打开文件

### rm 删除文件

- rm a.txt 删除文件
- rm abc -r 删除文件夹
- rm * (不能删除隐藏文件)删除所有文件

### cp 拷贝文件

- cp 1.txt 2.txt 
- cp abc abc_bak -r 拷贝文件夹

### 文件权限

- drwxrwxr-x d表示文件夹 - 表示文件
- rwx rwx r-x r可读 w可写 x执行
- 第一组指文件的拥有者的权限，第二组代表用户的组的权限，第三组代表其他用户的权限

| 选项 | 含义                                                         |
| ---- | ------------------------------------------------------------ |
| -a   | 该选项通常在复制目录使用时，它保留链接，文件属性，并递归地复制目录，简单来说，保持文件原有属性 |
| -f   | 覆盖已经存在地目标文件不提示                                 |
| -i   | 交互式复制，在覆盖目标文件之间将给出提示要求用户确认         |
| -r   | 若给出地源文件是目录文件，则cp将递归复制该目录下地所用子目录和文件，目标文件必须为一个目录名 |
| -v   | 显示拷贝进度                                                 |

### mv 移动，重命名

用户可以使用mv命令来移动文件或目录，也可以给文件或目录重命名。

```cmd
mv 1.txt 2.txt
```

常用选项说明：



| 选项 | 含义                                                         |
| ---- | :----------------------------------------------------------- |
| -f   | 禁止交互操作，如有覆盖也不会给出提示                         |
| -i   | 确认交互式操作，如果mv操作将导致对已存在地目标文件地覆盖，系统会询问是否重写，要求用户回答以避免误覆盖文件 |
| -v   | 显示移动进度                                                 |

### > 重定向

- ls > 1.txt 把命令返回地结果输出到文件中，会覆盖之间地数据，默认情况命令返回地结果是显示在屏幕中
- ls >> 1.txt 把命令返回地结果输出到文件中，追加地方式

### cat 查看文件内容

- cat 1.txt 把文件中地内容全部显示在屏幕中
- cat 1.txt 2.txt 
- cat 1.txt 2.txt > 3.txt 把多个文件地内容合并到新的文件中

### more 查看文件容，可以分页显示

- more 1.txt 查看内容多的文件，按空格往下翻页，按b键往回翻页，按q键退出

### 管道   |

- ls -l / | more 把左边的命令返回的结果交给右边命令进行处理

### ln 连接

**软链接 软链接本身不存储内容，只记录源文件的名称**

- ln -s 1.txt 1_link 给1.txt创建一个软链接，相当于windows中的快捷键方法
- ln -s 1.txt ~/1_link 给其他目录创建一个软链接，源文件不写绝对路径，软链接指向的文件是不存在的
- ln -s /home/shaojie/1.txt ~/1_link
- ln -s abc abc_link 给目录创建软链接

**硬链接 本身占空间，相当于把源文件复制一份，与源文件同步变化，删除硬链接文件不影响文件，不能给目录创建硬链接**

- 使用ll查看时，有一个数字，代表有几个文件能同步发生变化
- ln 1.txt 1_hard_link

### grep   查找文件内容

- grep hello test.txt 在文件总查找包含hello的内容，只要一行中有hello会把整行显示
- grep -niv hello test.txt n显示查找的内容的行号，i查找时不区分大小写，v反向查找，查找不包含hello的行
- grep -n hello /home/shaojie -r   查找整个目录中的所用文件，包含hello的内容

### find  查找文件

- find /home -name 1.txt 在/home目录下查找文件名为1.txt的文件
- find /home -name ·*txt·在/home目录下查找以txt结尾的文件

### tar   归档   打包

**打包**

- tar cvf a.tar 1.txt 2.txt f必须放在最后，f后面的第一个参数代表要生成的文件名，后面所有的参是要打包的文件

**解包**

- tar xvf a.tar 如果没写目录，把打包里面的文件解开到当前目录
- tar xvf a.tar -C tar 解包到指定的文件夹，文件夹需要提前创建好

###  gzip 压缩 

**压缩**

打包

​	tar cf a.tar * 生成a.tar文件

压缩

​	gzip -r a.tar 生成a.tar.gz文件

**解压**

解压

​	gzip -d a.tar.gz   生成a.tar文件

解包

​	tar xf a.tar -C ~/atar  解包到指定文件夹中

| 选项 | 含义           |
| ---- | -------------- |
| -d   | 解压           |
| -r   | 解压所有子目录 |

一步到位 打包并且压缩

tar czf b.tar.gz *txt 以gzip的方式打包并且压缩

一步到位 解压并且解包

tar zxf b.tar.gz -C btar 以gzip的方式解压并且解包

### bzip2

​	tar jcf j.tar.bz2 *txt 

​	tar xjf j.tar.bz2 -C jtar

### zip

​	zip -r zz *txt  zz表示生成的压缩文件，不需要写扩展名，会自动生成zip扩展名

​	unzip -d zz zz.zip   解压时会自动创建目录

#### 压缩率 zip < gzip<bzip2

#### 通用性 zip < gzip<bzip2

### 查看命令位置：which

###   su   切换到管理员账号

centos 下切换到root的简单命令：su

###   passwd 设置用户密码

在Unix/Linux中，超级用户可以使用passwd命令为普通用户设置或修改用户密码。用户也可以直接使用该命令来修改自己的密码，而无需再命令后面使用用户名

### exit  退出登录账户

- 如果时图形界面，退出当前终端
- 如果时使用ssh远程登录，退出登录账户
- 如果时切换后的登录用户，退出则返回上一个登录账号

### who 查看当前登录的用户

admin	pts/0

admin	tty1

pts 代表一个终端 tty代表用户登录了操作系统

pkill -kill -t tty1   杀死登录的用户

### reboot,shutdown  关机重启

| 命令               | 含义                                       |
| ------------------ | ------------------------------------------ |
| reboot             | 重新启动操作系统                           |
| shutdown -r now    | 重新启动操作系统，shutdown会给别的用户提示 |
| shutdown -h now    | 立刻关机，其中now相当于事件为0的状态       |
| shutdown -h 20：25 | 系统在今天的20：25会关机                   |
| shutdown -h +10    | 系统再过十分钟后自动关机                   |

### chmod   修改文件权限

**字母法**

- u 文件的拥有者
- g 文件的拥有的组
- o  其他用户
- a  所用用户

+添加权限

-删除权限

=设置权限（把之前的权限换成新的权限）

chmod u+r text.txt 给文件的拥有者添加r（读）权限

r  可读

w  可写

x  可执行

-没有任何权限

chmod u=r,g+w,o-r text.txt   给自己添加w权限，给其他人删除r权限

**数字法**

r   4

w   2

x   1

-0

chmod 123 test.txt    第一位数字代表自己的权限，第二位表示自己组的权限，第三位代表其他人的权限

3= 1+2 表示wx权限
